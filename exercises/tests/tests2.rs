// tests2.rs
//
// In this exercise, you're using `assert_eq!` to compare values. You should be
// able to replace `assert_eq!()` with the proper assertion in both places.
//
// Execute `rustlings hint tests2` or use the `hint` watch subcommand for a
// hint.



#[cfg(test)]
mod tests {
    #[test]
    fn you_can_assert_eq() {
        assert_eq!(1, 1);
    }
}
