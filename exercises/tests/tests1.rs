// tests1.rs
//
// In this exercise, you're using `assert!` to test a very basic boolean
// function. You should be able to replace `assert!()` with the proper
// assertion in both places.
//
// Execute `rustlings hint tests1` or use the `hint` watch subcommand for a
// hint.


#[cfg(test)]
mod tests {
    #[test]
    fn you_can_assert() {
        assert!(true);
    }
}
